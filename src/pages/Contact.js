import React from 'react';
import Navigation from '../components/Navigation';

const contact = () => {
    return (
        <div>
            <Navigation />
            c'est contact
        </div>
    );
};

export default contact;